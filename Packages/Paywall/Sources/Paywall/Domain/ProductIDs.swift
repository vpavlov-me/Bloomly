import Foundation

public enum ProductIDs {
    public static let monthly = "com.vibecoding.bloomly.premium.monthly"
    public static let yearly = "com.vibecoding.bloomly.premium.yearly"

    public static var all: [String] { [monthly, yearly] }
}
